/*Operadores aritméticos: São operadores binários, 
ou seja, operam em cima de dois operandos. Existem dois operandos.
*/

const [a, b, c, d] = [3, 5, 1, 15]
const soma = a + b + c + d
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2 //Para saber se é par ou ímpar, usar 'Módulo 2'

console.log(soma, subtracao, multiplicacao, divisao, modulo)

